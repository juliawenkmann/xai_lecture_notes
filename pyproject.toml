[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xai-book"
version = "0.1.0"
description = "Small reusable helpers for XAI lecture notebooks."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["xai_book*"]
